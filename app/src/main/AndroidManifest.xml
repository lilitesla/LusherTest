<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lili.tesla.lushertest">

    <application
        android:name=".presentation.application.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.screen.description.view.DescriptionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.screen.table1.view.Table1Activity" />
        <activity android:name=".presentation.screen.table2.view.Table2Activity" />
        <activity android:name=".presentation.screen.table3.view.Table3Activity" />
        <activity android:name=".presentation.screen.results.view.ResultActivity"></activity>
    </application>

</manifest>